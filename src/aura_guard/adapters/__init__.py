"""aura_guard.adapters

Framework integration adapters for Aura Guard.

Available adapters:
- openai_adapter: Helpers for OpenAI ChatCompletions-style message payloads
- langchain_adapter: LangChain callback handler (requires langchain-core)
- mcp_adapter: MCP (Model Context Protocol) server wrapper (requires mcp)
"""
